Act as an autonomous senior C++ coding agent. Produce one complete, self-contained C++20 source file implementing a durable embedded key-value database named CedarDB, together with an extensive built-in test suite and a command-line interface. The implementation must include: an append-only write-ahead log with checksums and crash-tail recovery; atomic batch put/delete operations; an in-memory ordered index; point lookup; prefix scan; snapshot sequence numbers; background-safe compaction expressed without external libraries; a binary file format with explicit little-endian encoding; robust short-read and short-write handling; fsync-based durability on POSIX; RAII file descriptors; clear error propagation; deterministic tests using a temporary directory supplied on the command line; corruption, truncation, recovery, compaction, snapshot, batch atomicity, and randomized model-based tests; and concise design comments explaining invariants. Do not use third-party dependencies. Do not emit Markdown fences, prose outside comments, pseudocode, placeholders, TODOs, omitted sections, or multiple files. Keep generating until the source itself is at least 16,000 model tokens long, while remaining valid compilable C++20. End only after the final closing brace, and append the comment // CEDARDB_COMPLETE as the last line.
